create database  `larissa`;
# a linha acima serve para criar um banco de dados com o nome: `Larissa`
# o uso da crase se torna necessário para indicar uma `string` do sistema
# `string` do sistema `database`, `table` e `campo`